# InternFlow Database Rules

## Database changes

Before modifying entities, repositories, migrations, seed data, or production init data, always read `INTERNFLOW_DATABASE_RULES.md`.

Do not change schema, seed/init data, or production DB config unless the task explicitly requires it.
